version: '2.4'

services:
  postgres:
    restart: always
    image: postgres:latest
    expose:
      - 5432
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: hackme
      POSTGRES_DB: staff
